# Portainer — variáveis de ambiente
# Copie este arquivo para .env e preencha os valores:
#   cp .env.example .env

# Hostname do Portainer (usado internamente)
PORTAINER_HOST=portainer.devdaniel.home.arpa
